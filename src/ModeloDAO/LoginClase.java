
package ModeloDAO;

public class LoginClase {
    
    /*
    public int Logearse(String myUser, String myPass){
        for (int i = 0; i < GestUsers.getUserCont(); i++) {
            if(myUser.equalsIgnoreCase(GestUsers.getUsuarios(i))){ //compara con todos los usuarios
                if(myPass.equals(GestUsers.getPasswords(i))){
                    return i; //devuelve el usuario que ha hecho match
                }
            }
            
        }
        return -1;
    
    } **********Eliminar!
    */
    
}
